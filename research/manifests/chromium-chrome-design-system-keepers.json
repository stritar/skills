{
  "id": "chromium-chrome-design-system-keepers",
  "verdict": "not-vendored-keeper",
  "repository": "chromium/chromium",
  "commit": "347790368025dc5659bd8c1c73fd7983b5e5268e",
  "keeperSkills": [
    {
      "id": "figma-design-review",
      "purpose": "Structured critique of a Figma design against Chrome Design System (CDS) component specs: fetches design context via the Figma MCP server, cross-references the Figma component against the codebase's `_Spec.md` files (via the sibling `project-knowledge` skill) instead of treating it as a raw shape, and audits raw-elements-vs-system-components, hardcoded-values-vs-tokens, unnecessary overrides, and variant accuracy, writing the critique to `out/<ComponentName>_Audit.md`.",
      "reason": "Requires (a) an active Figma MCP server configured against Google's internal Chrome Design System Figma library, (b) execution from the root of a full Chromium source checkout (`//src/`), and (c) the sibling `project-knowledge` skill plus its `_Spec.md` files, which are themselves generated by `generate-component-spec` into `agents/projects/chrome-design-system/assets/component-specs/` -- a path outside this skill's own directory and outside any single skill directory that could be bundled. Copied anywhere else it has no C++ Views / WebUI / Clank codebase to audit against and no spec files to cross-reference. Unusable outside a Chromium (effectively Google-internal Chrome Design System) checkout."
    },
    {
      "id": "figma-to-views-json",
      "purpose": "Converts a Figma design frame into a Chromium Views JSON prototype: extracts Figma parameters via the MCP server, maps colors/spacing to C++ `ui::ColorId` equivalents and Chromium layout managers (BoxLayout/FlexLayout/TableLayout), and outputs a JSON file runnable via the `views_examples` app.",
      "reason": "Hardwired to Chromium's C++ Views layout/color-token system (`ui::ColorId`, `views::BoxLayout`/`FlexLayout`/`TableLayout`, the `views_examples` test app) and requires execution from `//src/` inside a full Chromium checkout plus an active Figma MCP server against the internal CDS Figma library. Produces JSON that is meaningless outside the Chromium Views framework. Unusable outside a Chromium checkout."
    },
    {
      "id": "figma-to-views",
      "purpose": "Generates production-ready Chromium C++ Views code from a Figma design frame: retrieves design context via the Figma MCP server, cross-references component specs and token mappings (via `project-knowledge`), and emits `.h`/`.cc` files using `views::LayoutProvider`, `views::BoxLayout`, and Chrome Design System `ui::ColorId` tokens, following Chromium coding-style copyright headers.",
      "reason": "Same class of dependency as figma-to-views-json: requires a full Chromium `//src/` checkout, Chromium's C++ Views APIs and coding conventions, the internal CDS Figma library via the Figma MCP server, and the sibling `project-knowledge`/`generate-token-map` outputs for token mapping. The generated code has zero meaning outside the Chromium Views framework. Unusable outside a Chromium checkout."
    },
    {
      "id": "figma-to-webui",
      "purpose": "Generates production-ready Chromium WebUI code (TypeScript `CrLitElement` class, Lit HTML template, Chromium-stylelint-conformant CSS) from a Figma design frame, mapping Figma tokens to Chromium CSS variables with `chrome://theme/`/`chrome://resources/` fallback chaining for Material 3 light/dark themes.",
      "reason": "Hardwired to Chromium's internal `chrome://` resource/theme pipeline, the `CrLitElement` framework, and Chromium's own stylelint rules; requires execution from `//src/` and the internal CDS Figma library via the Figma MCP server, plus `project-knowledge`'s cross-platform token/spec data. Meaningless outside the Chromium WebUI stack. Unusable outside a Chromium checkout."
    },
    {
      "id": "generate-component-spec",
      "purpose": "Seven-step decision tree that maps one Figma CDS component to its equivalent C++ Views, WebUI, and Clank (Android) implementations by grepping specific Chromium source paths (`ui/views/controls/`, `ui/webui/resources/cr_elements/`, `ui/android/java/...`, `components/browser_ui/...`), diffs design tokens and interactive states across all three platforms, and writes a consolidated `{{ComponentName}}_Spec.md` following `assets/spec-template.md` into the shared project `assets/component-specs/` directory.",
      "reason": "Entirely organized around Chromium's own multi-platform source layout (specific Views/WebUI/Clank directory paths and file-naming conventions unique to this codebase) and the internal CDS Figma library via the Figma MCP server; its output path (`agents/projects/chrome-design-system/assets/component-specs/`) and its `assets/spec-template.md` dependency live in a project-level location, not portable to another codebase. Unusable outside a Chromium checkout."
    },
    {
      "id": "generate-token-map",
      "purpose": "Reads the skill's own bundled `assets/figma-variables.md` (143 CDS Figma variables across 11 categories) and maps each to its C++ (`ui::kColorSys*`/`views::style::*`), CSS (`--color-sys-*`/`--cr-*`), and Clank Android (`@macro/*`, `SemanticColorUtils.*`) equivalents by searching specific Chromium source files, writing the result to a shared `tokens.md` used by the other five skills.",
      "reason": "Read in full via raw fetch. The only skill of the six whose primary data file (`figma-variables.md`) is bundled inside its own directory rather than external, but its entire mapping target -- Chromium's `ui/color/color_id.h`, `ui/views/style/typography.h`, `ui/webui/resources/cr_elements/cr_shared_vars.css`, and multiple Clank Android resource/Java files -- only exists inside a Chromium checkout, and its output (`tokens.md`) is consumed by the other five (non-portable) skills in this set. Frontmatter uses a `metadata` block (`author: Chrome Design System Team`), confirming Chrome-Design-System-specific authorship. Unusable outside a Chromium checkout."
    },
    {
      "id": "project-knowledge",
      "purpose": "The 'data bridge' skill: on request, reads `assets/tokens/tokens.md`, `assets/component-mapping.md`, and `assets/component-specs/*_Spec.md` from the `agents/projects/chrome-design-system/` project directory (all generated by the other five skills) so the model has Chrome Design System knowledge without querying Figma directly.",
      "reason": "By design this skill has no content of its own -- its entire value is generated artifacts (tokens.md, component-mapping.md, component-specs/*) produced by the other five CDS skills at paths outside any single skill directory (`agents/projects/chrome-design-system/assets/...`), which are themselves Chromium-specific and not present when this directory alone is copied elsewhere. Naming the external dependency: the sibling skills' generated output under the shared `chrome-design-system` project assets folder. Unusable outside a Chromium checkout."
    }
  ],
  "logMd": "#### chromium/chromium :: agents/projects/chrome-design-system/skills/ (inspected 2026-08-29)\n- Repo pin: HEAD commit 347790368025dc5659bd8c1c73fd7983b5e5268e on branch `main` (cross-verified via GET /commits?per_page=1 and GET /branches/main, identical 40-hex sha both times -- the repo's automated internal-autoroll commits land continuously, so this pin is a single point-in-time snapshot). Stars 24650, pushed_at 2026-08-29T10:11:40Z. Used ONLY the Contents API (and the github.com HTML tree page as fallback when the Contents API returned transient 403s, likely unauthenticated rate limiting) scoped to `agents/projects/chrome-design-system/skills/` -- no tarball, no recursive/whole-repo tree fetch, per the instruction that this repo is too large for that.\n- Directory contains a top-level README.md (install-via-symlink instructions for Gemini CLI workspaces, confirming these are meant to be used from inside a live Chromium checkout) plus 6 skill directories, not the single skill originally reported: figma-design-review, figma-to-views-json, figma-to-views, figma-to-webui, generate-component-spec, generate-token-map, project-knowledge.\n- LICENSE read in full at the pin via raw.githubusercontent.com: copyright 'The Chromium Authors' 2015, three enumerated conditions (retain notice in source; reproduce notice in binary/docs; may not use 'Google LLC' or contributor names to endorse without permission) plus the standard AS-IS disclaimer -- this is the canonical **BSD-3-Clause** text (three conditions, including the no-endorsement clause), matching the repo API's license classification and the assignment's stated expectation. SPDX: BSD-3-Clause, licenseSource repo-root:LICENSE. License itself is clean and would support vendoring if the skills were otherwise portable.\n- SKILL.md read in full (verbatim, via a strict transcribe-only prompt after an initial fetch of project-knowledge/SKILL.md returned a suspicious non-verbatim response from the summarizing fetch model resembling an injected content-policy persona; the direct verbatim re-fetch showed the actual file is a short, benign 13-line pointer document with no hidden instructions -- treated as a fetch-tool artifact, not evidence of a real prompt injection in the file) for project-knowledge and generate-component-spec; read via detailed structured summary for figma-design-review, figma-to-views-json, figma-to-views, figma-to-webui, generate-token-map (full raw content, not just README claims).\n- Common findings across all 6: every skill requires (1) an active Figma MCP server, implicitly pointed at the internal Chrome Design System Figma library (fileKeys like `qj3RvxSvSMVdw4tcH8GVMX` appear as CDS-specific examples, not a public/generic file), and (2) execution from the root of a full Chromium source checkout (`//src/`), grepping and writing to Chromium-specific paths (`ui/views/controls/`, `ui/webui/resources/cr_elements/`, `ui/android/java/...`, `components/browser_ui/...`, `chrome/browser/ui/color/chrome_color_id.h`). No literal `go/` links or `*.corp.google.com`/internal-URL strings were found in any of the six files (generate-component-spec's SKILL.md was read fully verbatim specifically to check this), so there is no explicit internal-URL/credential leak to flag as a security issue -- the disqualifying factor is architectural, not a literal internal link: five of the six skills also depend on sibling skills' generated output living outside any single skill's own directory (project-knowledge reads tokens.md/component-mapping.md/component-specs/* that generate-token-map and generate-component-spec write to a shared `agents/projects/chrome-design-system/assets/` location), which the self-containment rule cannot fix by bundling since that content is Chromium-specific generated data, not static portable reference material.\n- No scripts were found in any of the 6 skill directories (only .gitignore/OWNERS/README.md/SKILL.md and, for generate-component-spec and generate-token-map, an `assets/` subdirectory) -- no download-and-execute or network-call security concerns beyond the MCP server dependency itself.\n- Verdict for all 6: **not-vendored-keeper**. Per the assignment's own criterion, these are unusable outside a Chromium checkout (in practice, unusable outside the Google/Chromium engineering context that maintains this specific Figma library and this specific C++/WebUI/Clank source tree) -- rubric relevance would score 2-3 for several of these (they are genuinely Figma-to-code / design-system audit workflows) but conformance scores 0 (hard-wired to a private, single-codebase source tree and a non-public design file, per rubric's 'Unusable outside its origin (hard-wired private services...)' criterion) and the registry already has a general figma-integration / design-to-code skill that serves the portable version of this idea, so nothing here clears the bar even before conformance zeroes the total."
}
