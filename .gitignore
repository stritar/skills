# The maintainer's private research archive (stritar/skills-research), cloned in place
research/

# Machine-local agent settings
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Node. The root tooling is zero-dependency and nothing installs at the root;
# the documentation site in site/ has its own package.json and lockfile.
node_modules/

# Documentation site: build output and content generated from skills/ by
# `npm run content` (scripts/lib/docs-content.mjs)
site/.next/
site/out/
site/.content/
site/public/raw/
site/public/llms.txt
site/next-env.d.ts
*.tsbuildinfo
